# دليل تحديث منصة كنوز — كيف تضيف وحدة أو عرضًا جديدًا

المنصة مبنية بلا أي أدوات معقدة: **ملفات + ملفات فهرسة JSON**. أي تعديل ترفعه إلى GitHub يظهر على الموقع خلال دقيقة تلقائيًا.

يمكنك التعديل بثلاث طرق: يدويًا من متصفحك (GitHub ← زر القلم ✏️ على أي ملف)، أو عبر أي مساعد ذكاء اصطناعي (أعطه هذا الدليل + الملف المطلوب تعديله)، أو عبر رفع ملفات جديدة من زر **Add file**.

---

## بنية المجلدات

```
site/
├── index.html / grade.html / unit.html / cambridge.html   ← صفحات الموقع (لا تعدّلها)
├── assets/        ← التصميم والمنطق (لا تعدّلها)
├── data/
│   ├── index.json      ← فهرس الصفوف والوحدات (تعدّله عند إضافة وحدة)
│   ├── g12-u1.json     ← فهرس عروض الوحدة 1 للصف 12
│   └── g12-u2.json     ← فهرس عروض الوحدة 2 للصف 12
└── files/
    └── g12/
        ├── u1/  decks/ (العروض pptx) + plans/ (الخطط docx) + ملف ZIP للوحدة
        └── u2/  decks/ + plans/ + ZIP
```

**قاعدة التسمية**: ملفات العروض على الخادم بأسماء إنجليزية قصيرة (مثل `ghurba-1.pptx` و`ghurba-1-plan.docx`)، والزائر يرى الاسم العربي عند التنزيل تلقائيًا.

---

## إضافة عرض جديد إلى وحدة موجودة (مثال: وحدة جديدة للصف 12)

### 1) ارفع الملفات
- العرض: إلى `files/g12/u2/decks/` باسم إنجليزي (مثل `istimaa-1.pptx`)
- الخطة: إلى `files/g12/u2/plans/` باسم `istimaa-1-plan.docx`

### 2) عدّل ملف الوحدة `data/g12-u2.json`
أضف عنصرًا داخل قائمة `"decks"` بنفس النمط:

```json
{
  "id": "istimaa-1",
  "title": "عنوان الدرس - الحصة الأولى",
  "lesson": "الاستماع: عنوان الدرس",
  "session": 1,
  "pages": "105-107",
  "slides": 12,
  "file": "files/g12/u2/decks/istimaa-1.pptx",
  "download": "عنوان الدرس - الحصة الأولى.pptx",
  "plan_file": "files/g12/u2/plans/istimaa-1-plan.docx",
  "plan_download": "خطة تحضير - عنوان الدرس - الحصة الأولى.docx"
}
```

> ملاحظة: يوجد حقل اختياري `"game_url"` لرابط لعبة المراجعة — لا تضفه الآن؛ سيُفعّل في مرحلة لاحقة عند إطلاق بنوك الألعاب على استضافة المنصة نفسها. عند غيابه لا يظهر زر اللعبة.
```

### 3) حدّث العدّادات في `data/index.json`
في عنصر الوحدة داخل الصف: غيّر `"decks"` و`"plans"` إلى العدد الجديد، وحدّث `"updated"` أعلى الملف بتاريخ اليوم.

### 4) ارفع التعديلات إلى GitHub — يُنشر تلقائيًا خلال دقيقة.

---

## إضافة وحدة جديدة كاملة (مثال: الوحدة الأولى للصف 10)

1. أنشئ المجلدات: `files/g10/u1/decks/` و`files/g10/u1/plans/` (ارفع فيها الملفات بأسماء إنجليزية).
2. انسخ ملف `data/g12-u1.json` إلى `data/g10-u1.json` وعدّل محتواه (المسارات تبدأ بـ `files/g10/u1/...`).
3. في `data/index.json` ابحث عن الصف العاشر، وغيّر `"status"` إلى `"ready"` وأضف داخل `"units"`:

```json
{"id":"g10-u1","unit":1,"title":"عنوان الوحدة","status":"ready",
 "manifest":"data/g10-u1.json","decks":10,"plans":10,"pages":"ص 8-30",
 "zip":"files/g10/u1/G10-الوحدة-الأولى.zip"}
```

4. (اختياري لكن مفضل) اصنع ملف ZIP للوحدة كاملة وارفعه بجانب المجلدات ليظهر زر «تحميل الوحدة كاملة».

---

## إخفاء وحدة أو تعليقها مؤقتًا
غيّر `"status"` من `"ready"` إلى `"soon"` — تبقى الملفات على الخادم لكنها لا تُعرض.

## ملاحظات مهمة
- احتفظ دائمًا بنسخة من أي ملف قبل تعديله (GitHub يحفظ تاريخ التعديلات ويمكنك التراجع من تبويب **History**).
- إن ظهرت صفحة بيضاء بعد تعديل JSON فالغالب فاصلة ناقصة أو زائدة — الصق الملف في <https://jsonlint.com> ليكشف الخطأ لك.
- لا ترفع ملفًا يتجاوز 100MB عبر متصفح GitHub (الملفات الحالية كلها أصغر من 1MB).
